<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.PhotoRecovery" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

    </style>

    <style name="Theme.PhotoRecovery" parent="Base.Theme.PhotoRecovery" />

    <!-- Bottom Sheet Dialog Start -->
    <style name="TransparentBottomSheetDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="bottomSheetStyle">@style/TransparentBottomSheetStyle</item>
    </style>

    <style name="TransparentBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
        <item name="behavior_peekHeight">0dp</item>
    </style>
    <!-- Bottom Sheet Dialog End -->

    <style name="PurchaseDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/admin_fade_in_purchase</item>
        <item name="android:windowExitAnimation">@anim/admin_fade_out_purchase</item>
    </style>


</resources>