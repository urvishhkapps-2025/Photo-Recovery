<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".activity.fragment.PhotoFragment">

    <RelativeLayout
        android:id="@+id/layView"
        android:visibility="gone"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:id="@+id/layAll"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_marginHorizontal="15dp"
            android:gravity="center"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/textAllSelect"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="10dp"
                android:fontFamily="@font/a_400"
                android:includeFontPadding="false"
                android:text="Select All"
                android:textColor="@color/appColor"
                android:textSize="16dp" />

            <CheckBox
                android:id="@+id/cbPhoto"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_alignParentEnd="true"
                android:background="@drawable/custom_check_box"
                android:backgroundTint="@color/transparent_color"
                android:backgroundTintMode="src_atop"
                android:buttonTint="@color/transparent_color"
                android:includeFontPadding="false" />

        </LinearLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@+id/layAll"
            android:layout_marginHorizontal="15dp"
            android:layout_marginVertical="10dp" />

    </RelativeLayout>

    <RelativeLayout
        android:visibility="gone"
        android:id="@+id/layNotView"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/imgNoFound"
            android:layout_marginTop="100dp"
            android:layout_width="wrap_content"
            android:src="@drawable/ic_item_no_found"
            android:layout_centerHorizontal="true"
            android:layout_height="wrap_content"/>

        <TextView
            android:id="@+id/textNoFount"
            android:layout_width="wrap_content"
            android:textSize="16sp"
            android:layout_marginTop="20dp"
            android:fontFamily="@font/a_500"
            android:includeFontPadding="false"
            android:textColor="#7E7E7E"
            android:layout_below="@+id/imgNoFound"
            android:layout_centerHorizontal="true"
            android:text="No Photos are found here..."
            android:layout_height="wrap_content"/>

    </RelativeLayout>

</RelativeLayout>